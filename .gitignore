# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.eggs/
.venv/
venv/
ENV/

# Note: .state/ is intentionally tracked — compute_starlink_metrics.py keeps the
# cumulative set of decayed Starlinks there, and CI needs it to persist between runs.

# OS
.DS_Store
Thumbs.db

# Secrets (local only)
.env
